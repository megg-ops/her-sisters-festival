# 《她的姊妹节》技术选型 ADR

> 文档版本：v0.2（2026-08-29 修订）
> 状态：除标注「待拍板」的条目外均为已定决策；**ADR-001、ADR-008 已被取代（Superseded）**，见各条说明
> 上位约束：PRD §17 技术假设、§16 无障碍、§18 验收标准；贵客松提交要求（可访问 URL、评委现场可玩）
> **v0.2 决策转向（2026-08-29）**：demo 与剧本已出现偏移，最终**以 `她的姊妹节-demo.html`（现约 2460 行单文件）为准，就地优化，不做 Vite/TS 迁移**。原 ADR-001（Vite+TS 重构）与 ADR-008（按模块从 demo 抽取重写）在提交/评审窗口内投入产出为负，且与 README 主打的「单文件、无构建、无框架、双击即玩、断网可玩」卖点冲突，故标为 Superseded、推迟至赛后若进入长期运营再重启。ADR-002/003/004/005/007 与单文件路线不冲突，保留；ADR-006 部署方式修正为不经 Vite 的静态托管。
> 就地优化对象：`她的姊妹节-demo.html`（单文件白盒实现，点击式交互，无角色移动）

---

## ADR-001 工程形态：~~Vite + TypeScript，不引入 UI 框架~~ → **已被取代（Superseded，2026-08-29）**

**状态**：~~已定~~ **已被取代**——保持原生单文件 HTML + 内联 JS，不引入 Vite / TypeScript / 任何 UI 框架。
**取代理由（2026-08-29）**：
- 提交/评审窗口内（8/29 提交、8/30 评审），Vite+TS 重构是纯内部改造，评委不可见，收益为零而回归风险高。
- README 已把「单文件、无构建、无框架」作为核心卖点，且「协议感知降级」（检测 `file://` 自动走离线预设）正依赖单文件形态天然成立。
- MVP 实际落地为 2 个实景（室内木楼 + 室外江畔）+ intro/结尾浮层，而非原设想的 8 场景，单文件复杂度可控。
**当前决策**：
- 无构建步骤，`她的姊妹节-demo.html` 即交付本体；`api/ask.ts` 作为 Vercel Serverless Function 独立存在。
- 原「`.impeccable/design.json` 组件 CSS 平移为 `src/styles/`」改为：CSS 内联于单文件 `<style>`，design.json 仍是唯一样式依据，防止风格漂移。
**原决策（存档，赛后若长期运营再重启）**：Vite（零配置、TS 开箱、产物静态）+ TypeScript（状态/场景结构多，类型防错）+ 不用 React/Vue/Svelte。
**后果**：赛前维护成本换取最小依赖面与部署最简；单文件行数增长是已知代价，赛后进入「章节地图」愿景时再评估模块化拆分。

## ADR-002 渲染方案：DOM/CSS 分层 + 绝对定位热点，不引入 Canvas 与游戏引擎

**状态**：已定
**背景**：PRD §17 允许 DOM/CSS 与 Canvas 组合，但明确"不为 MVP 新增大型游戏引擎"。本作无自由移动、无碰撞、无镜头跟随（重构任务单已确认不做 WASD）。
**决策**：
- 场景 = 背景图 `<div>` + 立绘层 `<img>`（绝对定位、固定站位）+ 热点按钮层（代码叠加，不烘焙进图，DESIGN.md）。
- 转场、蒸汽、织纹噪点 = CSS 动画 / SVG。
- 人物换装 = 透明 PNG 层叠（同一素体锚点，z-index 分层）。
**后果**：无障碍（真实 button、焦点、字幕）几乎零成本达成，符合 §16；放弃的是粒子类效果，MVP 不需要。

## ADR-003 状态管理：单会话内存状态 + localStorage 仅存偏好

**状态**：已定
**背景**：PRD §14.1 定义会话状态；§18.3 明确"刷新页面＝重新开始并给出提示"，无需持久化剧情进度。
**决策**：
- 剧情状态（`mealComplete`/`outfitComplete`/`dialogueComplete`/`moodColor`/`outfitChoices`/`askedObjects`/`echoText`/`giftIntent`）全部内存态，`state.ts` 单一来源，`initialState()` 工厂复位。
- `localStorage` 只存：音量/字幕/减少动态偏好（重新体验时保留，PRD §15）。
- 不用 Redux/状态库；场景与任务进度由状态推导渲染（延续 demo 的 `renderTasks` 模式）。
**后果**："再体验一次"＝重新调用 `initialState()`，天然满足连续演示复位要求；刷新丢失进度是已声明的设计而非缺陷。

## ADR-004 AI 服务：Vercel Serverless 代理 + 6 秒超时 + 本地预设兜底

**状态**：模型选择**待拍板**，架构已定
**背景**：PRD §17 要求密钥不出服务端；§8.4 规定超时/离线兜底矩阵。本机模型盘点（2026-08-28）：dashscope 文本模型在清单内多为过期预览；openai-next 网关整体 2026-08-31 失效——覆盖 8/29 提交与 8/30 现场评审，赛后失效由预设兜底承接。
**决策**：
- 架构：前端 `fetch /api/ask`（与 `/api/echo`）→ Vercel Serverless Function（Node runtime）→ 上游网关；密钥只存在于 Vercel 环境变量。
- **推荐模型：`qwen3-max`（经 openai-next 网关，OpenAI 兼容协议）**——中文文化语境表现好、协议接入最省；回声诗同模型。
- 超时：服务端 6s 硬超时；前端并行持有该物件的预设回答，超时即无缝切换并打「离线资料」标签（剧情文档 §3.6）。
- 输入防护：长度限制、不复述不当输入（预设话术兜底），不做画像（PRD §14.2）。
**备选**：gemini-3.5-flash（快但中文文化细节弱于 qwen）；dashscope 直连（清单内文本模型状态不稳，赛后如需长期运营再评估）。
**运维步骤**：Vercel 项目环境变量配置 `AI_GATEWAY_KEY`（我提供值，你在 Vercel 控制台填入，密钥不进仓库）。

## ADR-005 知识库：仓库内结构化 JSON，前后端同构读取

**状态**：已定
**背景**：PRD §9.1 规定知识条目字段；离线兜底要求前端本地可读。
**决策**：
- `src/data/knowledge.json`：条目含 `id`、主题、适用场景、已审核事实、边界、来源机构/标题/链接、审核状态。首批 5 物件 ×3 问直接取自剧情文本 §三。
- 前端构建时内联（离线兜底与来源抽屉展示）；Serverless 端同文件作为回答上下文注入提示词（检索式：按物件 id 取条目，不做向量库）。
- 审核状态字段未过审的一律显示"原型资料 · 待当地复核"（DESIGN.md 抽屉规范）。
**后果**：无外部数据库依赖，知识条目可进 git 评审流；将来扩条目只需加 JSON。

## ADR-006 部署：Vercel（纯静态文件 + Serverless Functions 同仓，不经 Vite）

**状态**：已定（v0.2 修正部署方式）
**背景**：提交要求"可直接访问的 URL"；Freya 有 Vercel 账号。随 ADR-001 取代，不再有 Vite 构建产物。
**决策**：
- 仓库即部署源：**无构建步骤**——`她的姊妹节-demo.html`（及 `assets/`、`src/data/knowledge.json`）作为静态文件直接托管，`api/*.ts` 自动成为 Vercel Functions；GitHub 仓库连接 Vercel，main 分支自动部署。
- Vercel 项目 Framework Preset 选 **Other / 无框架**，Build Command 留空，Output Directory 指向仓库根（或将 demo 复制为 `index.html`）。
- 域名用 Vercel 免费子域即可（`her-sisters-festival.vercel.app` 类似）。
- **展位保底天然成立**：交付本体本身就是单文件 HTML，展位断网时本地双击 `file://` 打开即可玩（`AI_ENABLED` 检测到非 http 协议自动走离线预设，体验完整）。原「用 `vite-plugin-singlefile` 另出单文件包」一条随 Vite 取代而**取消**。
**运维步骤**：在 Vercel 导入仓库（Import → Framework 选 Other，Build 留空），环境变量按 ADR-004 配置。

## ADR-007 资产格式与加载

**状态**：已定
**背景**：DESIGN.md 要求导出 AVIF/WebP；现场网络不稳。
**决策**：
- 生图原图存 `assets/`（带同名 `.md` 记录，不随前端部署）；前端用图存 `public/assets/`，**WebP 为主**（AVIF 编码慢、时间紧，正式期再补）。
- 首屏只加载封面 + S1；进入室内前预加载 A3/A4/B 组（`<link rel=preload>` 或 JS 预载），转场遮罩遮盖加载间隙。
- 单图 ≤500KB 红线，超出就压缩重导。

## ADR-008 ~~迁移路径：按模块从 demo.html 抽取，不就地改造~~ → **已被取代（Superseded，2026-08-29）**

**状态**：~~已定~~ **已被取代**——不做迁移；`她的姊妹节-demo.html` 即生产本体，就地优化。
**取代理由（2026-08-29）**：
- demo 已端到端跑通并验证过全部交互（点击热点、任务检查、三段做饭、五层换装、AI 问答/兜底、抽屉、赠饭四心意），是可交付的完成态，不是"待迁移的原型"。
- 原计划的「迁移期两份代码并存」在评审窗口内是最大回归来源，收益（模块化、类型）全部落在赛后。
- 场景实际收敛为 2 个实景 + 浮层，`src/scenes/s0..s7.ts` 的八场景拆分前提已不成立。
**当前工作方式**：所有优化直接在单文件内进行。剧本文档保留双版本——原设计稿 `她的姊妹节-背景故事与剧情文本.md`（v0.1，原封归档不动）作为完整愿景留存；新增 `她的姊妹节-背景故事与剧情文本-实现版.md`（v0.2）为与 demo 逐场景一致的实现版，未实现内容删除或降级为赛后 P2，文末附《§六 与 v0.1 差异》。`archive/` 仅存早期视觉探索，不再作为"迁移目标目录"。
**原决策（存档，赛后重启参考）**：按 `state.ts / ui/ / scenes/ / minigames/ / ai/` 模块从 demo 逐场景抽取重写，以重构任务单 §四 为合并门槛。

---

## 目录结构（v0.2：单文件实际形态）

```
her-sisters-festival/
├─ 她的姊妹节-demo.html      # 生产本体：单文件游戏（HTML/CSS/JS 内联，无构建）
├─ api/
│  └─ ask.ts                 # 文化问答代理（Vercel Serverless，6s 超时）
├─ src/
│  └─ data/knowledge.json    # 文化知识库（前端离线兜底 + 服务端问答上下文同构）
├─ assets/                   # 生图资产（WebP）+ 同名 .md 资产记录（部署时随静态托管）
├─ archive/                  # 早期视觉探索样张，留档，不作为生产依据
└─ （PRD / 剧情文本 / 设计系统 / 资产清单等文档）
```

> 注：`api/echo.ts`（回声诗 AI 生成）在 v0.2 **未实现**——demo 的回声/对歌环节为纯前端静态文案（3 意图听歌 + 明确标注「当代互动文案，不冒充传统苗歌」），符合 PRD §8.5 的「AI 不可用时用模板化组合」。如赛后接入，架构照 ADR-004 复制 `ask.ts` 即可。

## 待拍板清单（v0.2 更新）

1. **AI 模型**：推荐 `qwen3-max`（openai-next 网关）。架构不变，换模型只改网关配置。
2. ~~展位单文件保底包~~：**已消解**——交付本体即单文件，双击 `file://` 天然离线可玩。
3. Vercel 项目导入需要你登录操作（Framework 选 Other、Build 留空；或告诉我你已登录 Vercel CLI，我来连）。
4. `localStorage` 存偏好（ADR-003）：demo 现未落地（偏好仅存内存），是个小缺口——做/暂缓由你定。
