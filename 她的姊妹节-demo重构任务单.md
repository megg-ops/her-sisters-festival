# Demo 重构任务单（给执行 agent）

> 文档版本：v0.1（2026-08-28）
> 背景：现有 `她的姊妹节-demo.html` 是照 PRD §7.2"不做自由移动，点击热点切换"原文实现的点击式原型，问题不是 bug，是资产和对话呈现两块没跟上。经与 Freya 确认，**本轮不引入 WASD 移动/碰撞系统**，保留点击式交互，集中解决"看不到人物""对话没有临场感""场景是占位矢量图"三个问题。
> 依据：`DESIGN.md`（新增 Speech Bubble 组件）、`她的姊妹节-生图资产清单.md`（资产编号与生产顺序）、`她的姊妹节-背景故事与剧情文本.md`（对白内容）
> 验收人：Argo（本会话），改完请汇报改动点，不要自行判定"已通过"。

---

## 一、这次要改什么（3 件事，不做 WASD）

### 1. 场景背景：占位 SVG → 真实生图资产

- 替换对象：`她的姊妹节-demo.html` 里 `.scene-art` 的两个 `<svg>` 占位场景（"当代施洞木楼室内" / "清水江、山坡、节日场地"）。
- 替换素材：按生图资产清单"第一批"顺序——**A2（清晨卧房）/ A3（堂屋枢纽）/ A4（厨房近景）/ A8（节日场地）**，资产未就绪前，demo 结构先预留 `background-image` 挂载点，不要硬编码等资产。
- 资产命名与路径见生图资产清单 §一 命名规范：`assets/scene/名称-vN.png`。

### 2. 人物：占位轮廓 → 静态立绘

- 外婆：用 B11 立绘，固定站位在堂屋火塘边（A3 场景内的固定坐标，不需要移动逻辑）。
- 女主：用 B1（基底）+ B2（对应场景的姿势态），固定站位。
- 妈妈、阿月、赠饭对象：按剧情文本出场场景各自固定站位，用 B12/B13/B14。
- 这些立绘全部是**静态站位**，不做行走动画、不做移动交互，只是把"抽象色块"换成"看得出是谁的立绘"。

### 3. 对话：底部通栏 → 按 DESIGN.md 新分工拆成两条通道

DESIGN.md 已新增 `Speech Bubble` 组件（见该文档 Components 一节），分工规则：

- **同屏能看到具体 NPC 立绘时**（外婆、妈妈、阿月、赠饭对象在场）→ 用 Speech Bubble，气泡挂在该 NPC 立绘头顶偏上方，参数见 DESIGN.md Speech Bubble 小节（锚定、样式、六态、与 Dialogue Band 互斥显示）。
- **旁白/内心独白/无明确说话对象**（比如"木楼里还很安静，光是从窗子先进来的"这类）→ 继续用现有 `.dialogue-band` 底部通栏，不用改。
- 现有 demo.html 里凡是外婆/妈妈的对白（`say("外婆", ...)` 这类调用）都要从走 `dialogueBand` 改成走新的气泡渲染路径；旁白调用不用动。
- AI 文化问答（`renderCulture`/`setDrawer` 相关逻辑）的展开锚点也要从"对话带上方"改成"外婆气泡上方"，参照 DESIGN.md AI Cultural Dialogue 小节已同步的措辞。

## 二、明确不做的事（避免范围膨胀）

- 不做 WASD / 方向键移动。
- 不做角色行走动画、不做碰撞检测、不做镜头跟随。
- 不做户外场景的多 NPC 自由走位——阿月、赠饭对象固定站位即可。
- 环境音效（厨房蒸汽/人群/脚步等）已从声音需求里砍掉，demo 不需要接这些。

## 三、建议执行顺序（不阻塞等资产）

1. **先做气泡组件本身**（纯前端，不依赖生图资产）：用现有占位色块站位测试气泡出现/锚定/六态是否顺手，对齐 DESIGN.md 参数。
2. **资产到位后**依次接入：人物立绘 → 场景背景图，按生图资产清单的生产顺序来，不要等全部资产齐了才开始接（先出的先接，跑通一个场景就验证一次）。
3. 全部接完后，跑一遍 S1→S2→S3→S4→S5→S6→S7 全流程，确认气泡/对话带切换正常、没有两者同屏叠加的情况。

## 四、验收标准（Argo 会照这个查）

- [ ] 打开 demo，堂屋场景能看到外婆的立绘站在火塘边，不是抽象色块。
- [ ] 点击外婆/相关文化物件后，对话从外婆身上冒出气泡，不是走底部通栏。
- [ ] 旁白/独白依然走底部通栏，没有被错误改成气泡。
- [ ] 同一时刻气泡和对话带不会同屏叠加出现。
- [ ] 场景背景至少替换了 A3（堂屋）一张为真实生图资产（其余按到货顺序补）。
- [ ] 没有引入任何键盘移动逻辑（保持点击式，范围没有偷偷扩大）。
